# 기본: 텍스트 자동 정규화
* text=auto eol=lf

# 셸 스크립트는 반드시 LF (Linux/EC2 실행)
*.sh   text eol=lf
*.mjs  text eol=lf
*.js   text eol=lf
*.json text eol=lf

# PowerShell 은 CRLF 유지 (Windows 실행)
*.ps1  text eol=crlf

# 바이너리로 취급(만약 들어오면 diff 안 함) — 단 zip은 .gitignore로 커밋 안 됨
*.png  binary
*.jpg  binary
